Im Unterordner:
requirements installieren per pip
ausführen:
gen-csca-dsc.sh
keys_to_db.sh

Hier:
requirements installieren per pip
per konsole:
export FLASK_ENV=development
export FLASK_APP=server.py
echo web: gunicorn app:server > Procfile (?)
start.sh starten